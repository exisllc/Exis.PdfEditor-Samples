# Sample PDFs

This directory is reserved for sample PDF files used by the demo applications.

> **Note:** The Console demos are fully self-contained — they generate their own sample PDFs at runtime using `PdfBuilder`, so no pre-existing samples are required.

If you want to test with your own PDFs, place them in this directory and reference them from the demo applications.
